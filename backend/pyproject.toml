[project]
name = "appstock-backend"
version = "0.1.0"
description = "Backend local para AppStock"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.116.1",
    "uvicorn[standard]==0.35.0",
    "pydantic==2.11.7",
    "pyserial==3.5",
]

[tool.uv]
package = false
